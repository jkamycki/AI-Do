{
  "name": "aido",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run build --filter artifacts/aido",
    "dev": "cd artifacts/aido && pnpm run dev",
    "typecheck": "tsc -p artifacts/aido/tsconfig.json --noEmit"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
