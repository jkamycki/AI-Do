{
  "name": "aido",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "cd artifacts/aido && npm run build",
    "dev": "cd artifacts/aido && npm run dev",
    "typecheck": "tsc -p artifacts/aido/tsconfig.json --noEmit"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
